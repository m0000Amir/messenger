<aside class="chats-list">
    {{{profileLink}}}

    {{{ search }}}

    {{{ addChat }}}

    {{#if isLoaded }}
        {{#each chats}}
            {{{this}}}
        {{/each}}

    {{else}}
        Loading...
    {{/if}}

    {{{ addChatPopup }}}
</aside>
